# Ternary Operators

# Ternary operators are a way to write if-else statements in one line
# Syntax: <value if true> if <condition> else <value if false>

# Example:
buddy = "dog"
print(f"Hey {buddy}" if buddy == "dog" else "buddy is not a dog")
